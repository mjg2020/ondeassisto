# Modelo das variáveis de ambiente. Este arquivo É versionado — nunca coloque
# valores reais aqui. Copie para .env.local e preencha lá.
#
#   cp .env.example .env.local

# TMDB — API Read Access Token (v4), header Authorization: Bearer.
# Obtenha em https://www.themoviedb.org/settings/api
TMDB_READ_TOKEN=
